[project]
name = "clawpot-integration-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0",
    "pytest-timeout>=2.3",
]

[tool.pytest.ini_options]
testpaths = ["."]
timeout = 120
log_cli = true
log_cli_level = "INFO"
